# Android 签名密钥配置模板
# 使用方法：复制为 key.properties，并填入真实值
# key.properties 已加入 .gitignore，不会提交到仓库

storeFile=../keystore/release.keystore
storePassword=your_store_password
keyAlias=release
keyPassword=your_key_password
