# 21. 开发路线图

> 本文档是 TCM-History-AI 平台从「设计完成」走向「产品交付」的迭代路线图，对齐 [00-项目简介](./00-项目简介.md) §6.5 的「可直接进入开发阶段」声明与 [20-商业化方案](./20-商业化方案.md) §20.11 的三阶段商业化节奏。

---

## 21.1 路线图总览

| 阶段 | 周期 | 主题 | 交付门槛 |
| ---- | ---- | ---- | -------- |
| P0 | 2026-07 | 工程骨架 + AI 真实接入 | 7 服务可编译、AI Service LLM 走真实 OpenAI/Anthropic 协议 |
| P1 | 2026-08 | 存储与事件链路打通 | Milvus / Neo4j / RabbitMQ 消费循环全部接入真实协议，知识图谱 ETL 端到端可跑 |
| P2 | 2026-09 | 业务闭环（history 已达） | History Service 八大实体 CRUD + 全文检索 + 文件上传全链路联调通过 |
| P3 | 2026-10 | RAG 与 Agent 联调 | 文献上传 → 切片 → 向量化 → 检索 → 重排 → Agent 答案端到端跑通 |
| P4 | 2026-11 | 学习与用户域补齐 | Learning Service 课程/考试/错题、User Service 认证/权限全链路 |
| P5 | 2026-12 | 前端双端联调 | admin 后台与 learner 学习端覆盖核心页面，对接所有后端接口 |
| P6 | 2027-Q1 | 移动端 MVP | Flutter 学习端首页/搜索/详情/我的五个核心页面 |
| P7 | 2027-Q1 | 生产部署 | Kubernetes + Helm 生产编排，监控告警，灰度发布 |
| P8 | 2027-Q2 | 商业化试运营 | 会员体系、API 开放平台、5–10 所院校试点 |

---

## 21.2 阶段拆解

### P0 — 工程骨架与 AI 真实接入（已完成）

**目标**：把「设计说明书」落地为可编译、可运行的工程骨架。

**交付物**：
- 后端 7 服务（gateway + user / history / knowledge / graph / ai / learning）Clean Architecture + DDD 落地
- 共享 `pkg/`（config / errno / gormutil / idgen / logger / outbox / pagination / rabbitmq / response / tracing）
- `deploy/docker/docker-compose.dev.yml` 一键拉起 8 个依赖（PG / Redis / Neo4j / Milvus / MinIO / Meilisearch / RabbitMQ / etcd）
- GitHub Actions 四条流水线（backend / frontend / mobile / commitlint）
- AI Service 的 LLM Provider 接入真实 HTTP 协议：OpenAI、Anthropic、DeepSeek、Qwen（DashScope 兼容模式）、Kimi、GLM

**验收标准**：`make backend-build` 全绿；`make backend-test` 通过；任一服务本地 `make run-*` 可启动并响应 `/health`。

---

### P1 — 存储与事件链路打通（已完成）

**目标**：把 knowledge-service / graph-service / ai-service 的三类「stub 占位」替换为真实协议客户端，让数据真正流动起来。

**任务清单**：

- [x] **knowledge-service · Embedding**：接入 OpenAI `text-embedding-3` HTTP API（沿用 ai-service `net/http` 直连模式，不引入 vendor SDK），保留 `provider=stub` 离线回退。
- [x] **knowledge-service · Milvus**：接入 Milvus REST API v2（`/v2/vectordb/entities/insert|delete|search`），实现 `EnsureCollection / Insert / DeleteByDoc / Search`，保留 `enabled=false` 内存回退。
- [x] **graph-service · Neo4j**：接入 Neo4j HTTP 事务 API（`POST /db/{db}/tx/commit` + Cypher），实现 8 类节点 MERGE / 9 类关系 MERGE / 复杂图查询，保留 `enabled=false` 内存回退。
- [x] **graph-service · RabbitMQ Subscriber**：补全 `Subscribe` 消费循环（dial → channel → queue declare → bind routing keys → consume → dispatch to handler），实现 nack 语义与 best-effort 启动。

**验收标准**：本地 `docker compose up` 拉起真实 Milvus / Neo4j / RabbitMQ 后，把对应 `enabled` 置 `true`，知识图谱 ETL 链路（`doc.indexed` 事件 → Neo4j 节点 upsert）端到端可跑。

---

### P2 — 业务闭环：History Service（已完成）

**目标**：中医发展史八大实体（朝代 / 学派 / 人物 / 著作 / 事件 / 方剂 / 药物 / 疾病）业务全链路。

**交付物**：43 条 HTTP 路由、12 张数据表、4 张关联表、Meilisearch 跨实体检索、MinIO 文件上传、RabbitMQ 领域事件发布。

**验收标准**：根 README 第 105 行标注的「History Service（已完成 P2）」。

---

### P3 — RAG 与 Agent 联调（已完成）

**目标**：把 [06-RAG设计](./06-RAG设计.md) 与 [07-Agent设计](./07-Agent设计.md) 的端到端链路跑通。

**任务清单**：

- [x] 文献上传 → MinIO 原文桶 → Markdown 抽取 → 切片 → 向量化 → Milvus 入库（knowledge-service `IngestUseCase`）
- [x] 用户提问 → query 向量化 → Milvus ANN + Meilisearch BM25 → RRF 融合 → 重排 → Agent Plan→Execute→Answer（knowledge-service `RetrievalUseCase` + ai-service `AgentUseCase`）
- [x] Agent 调用 Knowledge Service / Graph Service / Tool 的多阶段执行链路（rag / graph / tool / direct 四通道）
- [x] Prompt Center 模板治理（CRUD + 激活 + 变量渲染 + 场景化 Prompt）

**验收标准**：上传《伤寒论》一份样本文献后，对「伤寒论中太阳病的代表方剂是什么」能给出有引用、有图谱关联、可追溯的答案。

---

### P4 — 学习与用户域补齐（已完成）

**目标**：补齐 Learning Service 与 User Service 的业务实现，让平台具备「学习闭环」能力。

**任务清单**：

- [x] User Service：注册 / 登录 / JWT / RBAC 权限 / 个人资料 / 用户设置（含 admin 用户管理与角色权限管理）
- [x] Learning Service：课程 / 课时 / 选课 / 学习记录 / 考试 / 错题 / 学习计划 41 条路由
- [x] Learning Service Redis 缓存（学习进度、最近错题，带优雅降级）
- [x] 考试超时自动提交后台 worker（60s 扫描 + 30s 超时上下文）
- [x] AI 学习计划生成 + 降级 fallback（HTTP 调用 ai-service）
- [x] Learning Dashboard 聚合统计（选课/完成/学习时长/考试/学习计划）
- [x] Gateway RBAC 中间件（最长前缀匹配 + 角色校验）

**验收标准**：一个完整的学习者旅程——注册 → 选课 → 学习 → 考试 → 错题回顾 → 学习计划生成。

---

### P5 — 前端双端联调（基本完成）

**目标**：[12-前端设计](./12-前端设计.md) 落地，admin 后台与 learner 学习端覆盖核心页面。

**任务清单**：

- [x] 工程骨架：pnpm monorepo（apps/admin + apps/learner + packages/api + packages/shared + packages/stores）
- [x] admin：dashboard / history 八大实体 CRUD / ai 对话与 prompt / knowledge 文献管理 / learning 课程考试 / users 用户管理
- [x] learner：home 首页 / book 著作 / person 人物 / school 学派 / search 跨实体检索 / timeline 时间线
- [x] `packages/api` 封装 9 大模块 OpenAPI 接口（auth/user/history/knowledge/graph/ai/learning）
- [x] `packages/shared` design tokens 与 `packages/stores` Pinia 状态管理
- [x] 与 Gateway 的 JWT 认证联调（拦截器 + 401 自动刷新 + 路由守卫）
- [x] admin 端 history 八大实体完整 CRUD（朝代/学派/人物/著作/事件/方剂/药物/疾病）
- [x] learner 端 9 个视图页面与后端 API 联调（home/search/timeline/person/book/school）
- [ ] learner 端知识图谱浏览、文献检索详情、时间线交互增强

**验收标准**：admin 端可管理 history 八大实体并触发 AI 对话；learner 端可浏览知识图谱、检索文献、查看时间线。

---

### P6 — 移动端 MVP（API 联调完成）

**目标**：[13-移动端设计](./13-移动端设计.md) 落地，Flutter 学习端 MVP 上线。

**任务清单**：

- [x] 项目骨架：`mobile/` Flutter 工程、路由（go_router）、状态管理（Riverpod）、网络层（Dio + 拦截器）
- [x] 五个核心页面：首页 / 搜索 / 详情（人物/著作/学派）/ 时间线 / 我的
- [x] 认证模块：登录 / 注册 / JWT token 持久化（SharedPreferences）/ 认证守卫
- [x] 主题系统：Material 3 + 中医风格配色
- [x] 共享模型：Person / Book / School / Dynasty / Event / SearchHit / UserProfile
- [x] 与 Gateway 的 JWT 认证联调（Dio 拦截器 + TokenStorage）
- [x] 五个核心页面与后端真实接口联调（Home/Search/Detail/Timeline/Profile Provider 全部接入 Repository）
- [ ] iOS / Android 双端构建产物

**验收标准**：移动端可登录、浏览首页、搜索实体、查看详情、查看时间线。

---

### P7 — 生产部署（基本完成）

**目标**：[16-Kubernetes部署](./16-Kubernetes部署.md) 落地，生产级 K8s 编排上线。

**任务清单**：

- [x] `deploy/k8s/` 命名空间 / Deployment / Service / Ingress / ConfigMap / Secret / HPA（26 个 yaml 文件）
- [x] `deploy/helm/` Helm Chart（values.dev.yaml / values.prod.yaml，20+ 个 templates）
- [x] 8 个依赖中间件的 StatefulSet / 持久化卷（PG / Redis / Neo4j / Milvus / MinIO / Meilisearch / RabbitMQ）
- [x] 三探针配置（startup / readiness / liveness）
- [x] 滚动发布（`maxUnavailable: 0` + `preStop sleep 15` + `revisionHistoryLimit: 10`）
- [x] Prometheus + Grafana 监控、Loki 日志、Jaeger 链路追踪（K8s 清单 + Helm 模板，values开关）
- [x] 金丝雀灰度发布策略（Deployment 副本数控制流量比例，Helm 模板）
- [x] 生产化网络策略（NetworkPolicy 默认拒绝 + Prometheus 抓取白名单）
- [x] 生产化备份方案（PostgreSQL + Neo4j 定时备份 CronJob + RBAC + PVC + 自动清理）
- [x] TLS 证书自动签发（cert-manager ClusterIssuer + HTTP-01 挑战）
- [x] StorageClass 配置（global.storageClass 参数化，生产 fast-ssd）
- [ ] 生产化 checklist 演练（Secret 外部化 / 镜像 tag 固定 / 备份恢复演练）

**验收标准**：`helm install tcm-history-ai ./deploy/helm/tcm-history-ai -f values.prod.yaml` 一键部署到 K8s 集群，所有 Pod Ready，健康检查通过。

---

### P8 — 商业化试运营（后端基本完成）

**目标**：[20-商业化方案](./20-商业化方案.md) 第一阶段（1–12 个月）落地。

**任务清单**：

- [x] 会员体系（套餐 / 订阅 / 订单，user-service 实现）
- [x] API 开放平台（API Key 管理 + 配额 + 限流，user-service 实现）
- [x] 支付回调接口（`POST /membership/payment/callback`，订单状态更新 + 订阅激活）
- [x] 订阅与 API Key 配额联动（订阅成功后自动同步 QuotaDaily/QuotaMonthly/RateLimitPerMinute）
- [ ] 学校版（5–10 所院校试点）
- [x] AI 成本可控性监控（token 计量、配额、限流，ai-service 实现）

**验收标准**：C 端注册 → 试用 → 付费转化漏斗可观测；B 端至少 1 所院校完成试点部署。

---

## 21.3 当前状态（截至 2026-07-27）

| 模块 | 状态 | 说明 |
| ---- | ---- | ---- |
| 后端 7 服务骨架 | ✅ 完成 | 全部可编译可运行，159 个测试文件 |
| AI Service LLM Provider | ✅ 完成 | OpenAI / Anthropic / DeepSeek / Qwen / Kimi / GLM 真实 HTTP 协议 |
| AI Service Tool Executor | ✅ 完成 | HTTP 调用注册 endpoint，含降级回退 |
| AI Service Agent 编排 | ✅ P3 完成 | Plan→Execute→Answer 四通道（rag/graph/tool/direct）+ transactional outbox |
| AI Service Prompt Center | ✅ P3 完成 | CRUD + 激活 + 变量渲染 + 场景化模板 |
| History Service | ✅ P2 完成 | 八大实体 CRUD + 检索 + 上传 + 事件 |
| Knowledge Service · Milvus | ✅ P1 完成 | REST API 客户端 + stub 回退，EnsureCollection/Insert/DeleteByDoc/Search 已实现 |
| Knowledge Service · Embedding | ✅ P1 完成 | OpenAI text-embedding-3 HTTP 客户端 + stub 回退 |
| Knowledge Service · RAG 流水线 | ✅ P3 完成 | Ingest 写入侧 + Retrieval 读取侧（vector+BM25+RRF+rerank）端到端 |
| Graph Service · Neo4j | ✅ P1 完成 | HTTP Transaction API + Cypher，11 个 GraphStore 方法 + 8 类节点约束 |
| Graph Service · RabbitMQ Subscriber | ✅ P1 完成 | 完整消费循环 + QoS + Nack 语义 + best-effort 启动 |
| Learning Service | ✅ P4 完成 | 课程/课时/选课/学习记录/考试/错题/学习计划/Dashboard，41 条路由 |
| Learning Service · Redis 缓存 | ✅ P4 完成 | 学习进度 + 最近错题，优雅降级 |
| Learning Service · 考试超时 | ✅ P4 完成 | 后台 worker 自动提交过期 attempt |
| Learning Service · AI 学习计划 | ✅ P4 完成 | HTTP 调用 ai-service + 降级 fallback |
| User Service | ✅ P4 完成 | 注册/登录/JWT/RBAC/资料/设置/管理员，23 条路由 |
| Gateway · RBAC 中间件 | ✅ P4 完成 | 最长前缀匹配 + 角色校验，admin 路由强制 admin 角色 |
| PC 前端 admin | ✅ P5 完成 | dashboard / history 八大实体 CRUD / ai / knowledge / learning / users 视图 |
| PC 前端 learner | ✅ P5 基本完成 | home / book / person / school / search / timeline 9 个视图 + API 联调 + JWT 认证 |
| PC 前端 packages | ✅ P5 完成 | api（9 模块，含完整 CRUD 类型）/ shared（design tokens）/ stores（Pinia + JWT 持久化） |
| 移动端 Flutter | ✅ P6 联调完成 | 五大 feature（home/search/detail/timeline/profile）+ Riverpod + Dio + GoRouter + 真实 API 联调 |
| K8s 部署清单 | ✅ P7 基本完成 | 26 个 yaml，7 后端 + 8 中间件 + ingress + HPA + 可观测性 + 金丝雀 + 网络策略 |
| Helm Chart | ✅ P7 基本完成 | 20+ templates + values.dev.yaml / values.prod.yaml，备份/TLS/可观测性/金丝雀/网络策略 values 开关 |
| 可观测性栈 | ✅ P7 清单完成 | Prometheus / Grafana / Loki / Jaeger，K8s 清单 + Helm 模板 |
| 灰度发布 | ✅ P7 清单完成 | 金丝雀 Deployment 副本比例控制，Helm 模板化 |
| 生产化备份 | ✅ P7 完成 | PostgreSQL + Neo4j 定时备份 CronJob + RBAC + PVC + 自动清理 |
| TLS 证书管理 | ✅ P7 完成 | cert-manager ClusterIssuer + HTTP-01 挑战自动签发 |
| 商业化 · 会员体系 | ✅ P8 后端完成 | 套餐/订阅/订单，user-service 12 条路由 |
| 商业化 · API 开放平台 | ✅ P8 后端完成 | API Key 管理 + 配额 + 限流，user-service 6 条路由 |
| 商业化 · 支付回调 | ✅ P8 后端完成 | 支付回调接口 + 订阅激活 + API Key 配额联动 |
| 商业化 · Token 计量 | ✅ P8 后端完成 | token 使用记录 + 月度配额 + 使用统计，ai-service 3 条路由 |
| CI/CD 流水线 | ✅ 完成 | backend / frontend / mobile / commitlint 四条 |

---

## 21.4 待完成清单（下一阶段优先级排序）

| 优先级 | 任务 | 所属阶段 | 预计工作量 |
| ------ | ---- | -------- | ---------- |
| P0 | 集成测试：端到端联调验证（Gateway + 7 服务 + 前端） | P5/P6 | 2 周 |
| P1 | learner 端知识图谱浏览、文献检索详情页 | P5 | 1 周 |
| P2 | 移动端 iOS / Android 双端构建产物发布 | P6 | 1 周 |
| P3 | 生产化 checklist 演练（Secret 外部化 / 备份恢复演练） | P7 | 3 天 |
| P4 | 商业化学校版（院校租户管理 + 数据隔离） | P8 | 2 周 |
| P5 | AI Agent 多轮对话记忆与上下文管理 | P3 | 1 周 |

---

## 21.5 风险与依赖

| 风险 | 影响 | 缓解措施 |
| ---- | ---- | -------- |
| 离线开发环境无法 `go get` 新模块 | 阻断 vendor SDK 接入 | 全部采用 `net/http` 直连 REST/HTTP 协议，与 ai-service LLM Provider 模式一致 |
| Milvus REST API schema 复杂 | 向量化入库链路阻塞 | P1 先做 Insert/Delete/Search，EnsureCollection 走 describe-or-create |
| 国产 LLM 厂商协议差异 | AI 多模型切换成本 | OpenAI 兼容协议统一覆盖 DeepSeek / Qwen / Kimi / GLM |
| 移动端与 PC 端 API 重复封装 | 维护成本 | 共享 `packages/api` TypeScript 定义，移动端独立 Dio 封装 |
| 商业化阶段 AI 成本不可控 | 毛利压力 | P8 上线 token 计量与配额限流，参考 [08-MCP设计](./08-MCP设计.md) §8.5 |

---

## 21.6 决策记录

- **ADR-21-01**：所有外部 SDK 接入优先采用 `net/http` 直连 HTTP/REST 协议，不引入 vendor Go SDK。理由：离线开发环境约束 + 协议透明可调试 + 与 ai-service 既有模式一致。
- **ADR-21-02**：所有 stub 客户端保留 `enabled=false` 内存回退路径。理由：离线开发与单元测试可在无外部依赖下运行，与既有 `milvus.enabled=false` / `neo4j.enabled=false` 配置开关一致。
- **ADR-21-03**：移动端采用 Flutter 而非 React Native。理由：[13-移动端设计](./13-移动端设计.md) 已确定，性能与一致性更优。
