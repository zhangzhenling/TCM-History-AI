// Package domain is reserved for the Gateway's domain layer. The gateway is
// a thin reverse proxy and currently has no business entities of its own;
// this file exists so the canonical layered layout matches the other services.
package domain
